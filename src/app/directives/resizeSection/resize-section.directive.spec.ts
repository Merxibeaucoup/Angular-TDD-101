import { ResizeSectionDirective } from './resize-section.directive';

describe('ResizeSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeSectionDirective();
    expect(directive).toBeTruthy();
  });
});
