import { ResizablecolumnDirective } from './resizablecolumn.directive';

describe('ResizablecolumnDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizablecolumnDirective();
    expect(directive).toBeTruthy();
  });
});
